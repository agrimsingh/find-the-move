# Slides waiting on locked copy

DO NOT IMPLEMENT THESE. Copy is not locked. This file is the placeholder.

Spoken slides 10–13 (will append to `export const slides`, which becomes length 13):

- `the-chart`
- `locked-test`
- `depreciation`
- `close`

Gated, not a spoken beat (`holding: true`; skipped by next/prev; `d` jumps here):

- `demo-fallback`

In `lib/slides.tsx`:

```
// TODO slides 10–13 + demo-fallback — waiting on locked copy
```

Do not invent titles, bodies, notes, $ amounts, token counts, or demo-fallback copy.
